name: api.lavylavender.com

services:
  api:
    container_name: api
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - stack.env
    command: >
      sh -c "pnpm run start:prod"
    environment:
      - REDIS_URL=redis://redis:6379/0
      - PORT=4000
    networks:
      - intranet
      - traefik-public
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped
    labels:
        - traefik.enable=true
        - traefik.constraint-label=traefik-public
        - traefik.docker.network=traefik-public

        - traefik.http.services.api.loadbalancer.server.port=4000
        - traefik.http.routers.api.rule=Host(`api.lavylavender.com`)
        - traefik.http.routers.my-router.middlewares=https-redirect@swarm
        - traefik.http.routers.api.service=api
      
        
        

  redis:
    container_name: redis
    image: redis:6.2-alpine
    restart: always
    networks:
      - intranet
    volumes:
      - redisdata:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 3

volumes:
  redisdata:


networks:
  intranet:
    driver: bridge
  traefik-public:
    external: true
